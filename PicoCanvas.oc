// PicoCanvas - Pixel art!
// started 23/02/2026

// Global variables
Event e;
Draw draw;
int gridSize, toolSelected;
int tempCanvas[1024];
bool soundEnabled = false;

//Global variables for canvas form
int pixelColorIndex,
	prevPixelColorIndex,
	eraserColorIndex;

handler PicoCanvas.onstart() {
	// load the main form
	mainForm.load();
}
